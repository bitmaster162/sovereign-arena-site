{
  "name": "codex04-r51-sovereign-arena-truth-repair",
  "version": "0.0.0-rnext-local-1",
  "private": true,
  "type": "module",
  "scripts": {
    "qualify:claims": "node tools/qualify_claims.mjs",
    "build": "node tools/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run overlay:rnext && npm run build && npm test",
    "overlay:rnext": "node tools/apply_rnext_overlay.mjs",
    "verify:r51-anchor": "npm run qualify:claims && npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
